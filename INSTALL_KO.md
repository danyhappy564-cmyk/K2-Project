# K2 Project 0.1.0 — 서버 시험판

SPT 4.1.5용 첫 서버 구현입니다. K2 전용 외형은 아직 없으며 HK416 외형·애니메이션·부품을 사용합니다.
추가되는 무기는 `대우정밀 K2 5.56×45 돌격소총 (시험용)`입니다. 기존 HK416은 변경하지 않습니다.

## 설치와 획득

게임과 서버를 종료하고 배포 ZIP의 `SPT_Runtime` 폴더를 SPT 최상위 폴더에 복사합니다.
최종 경로: `SPT_Runtime/user/mods/K2-Project/K2.Server.dll`.
같은 폴더의 `data`도 반드시 함께 복사해야 합니다. 별도 BepInEx 플러그인이나 WTT 의존성은 없습니다.

서버 시작 시 `[K2] 시험용 무기 등록 완료` 로그를 확인합니다.
피스키퍼 우호도 1에서 완성품을 500달러에 구매할 수 있습니다. 탄창은 비어 있으며 호환 5.56×45 탄약을 별도로 장전합니다.
연사속도는 750 RPM이며 나머지 물리 특성은 HK416을 상속합니다. 가격은 시험용 임시값입니다.
플리마켓·봇 장비·월드 스폰은 이번 단계에 추가하지 않습니다. F12 설정은 없습니다.

## 확인할 동작

상품 미리보기, 구매, 주무기 슬롯 장착, 탄창 장전·교체, 단발·연발, 레이드 진입·탈출을 확인해야 합니다.
서버 검증과 컴파일 성공만으로 실제 게임의 애니메이션과 레이드 동작까지 검증되지는 않습니다.
모드 삭제 전에 프로필을 백업하고 시험용 K2 및 저장한 K2 프리셋을 정리하세요.

## 이후 작업

README의 GAMGO 원본 모델 확보 → Blender 부품 분리·리깅 → Unity 프리팹·번들 제작 → 전용 슬롯·부품 등록 → 인게임 검증.
전용 모델을 적용할 때에는 HK416 슬롯 구조와 K2 부품 구조의 차이도 함께 수정해야 합니다.
현재 배포물에는 Sketchfab 모델이나 게임 원본 번들을 포함하지 않습니다.
원본 모델의 라이선스와 크레딧은 저장소 README 및 각 모델의 license.txt를 확인합니다.

프로젝트 기획: C33 / R_F. 서버 시험판 구현: Codex.
무기 ID: `d21900000000000000000001`.
빌드: PowerShell에서 `./Build-Release.ps1`. 데이터 재생성: `./Server/Generate-Data.ps1`.
